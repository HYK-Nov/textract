<Window x:Class="Textract.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Textract"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" WindowState="Maximized" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <StackPanel   Orientation="Horizontal" >
            <Button Content="이미지 불러오기" Click="LoadImage_Click" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="10"/>
            <Button Content="OCR 실행" Click="RunOcr_Click" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="10"/>
        </StackPanel>

        <Grid Grid.Row="1" Margin="10,0,10,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="70*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="29*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0"  x:Name="ImageContainer">
                <Image x:Name="MainImage"
                       Stretch="Uniform"
                       HorizontalAlignment="Stretch"
                       VerticalAlignment="Stretch"/>

                <Canvas x:Name="OverlayCanvas"
                        Background="Transparent"
                        MouseLeftButtonDown="OverlayCanvas_MouseLeftButtonDown"
                        MouseMove="OverlayCanvas_MouseMove"
                        MouseLeftButtonUp="OverlayCanvas_MouseLeftButtonUp">
                    <Rectangle x:Name="SelectionRect"
                               Stroke="Red"
                               StrokeThickness="2"
                               Fill="#40FF0000"
                               Visibility="Collapsed"/>
                </Canvas>
            </Grid>
            <TextBox x:Name="OcrResultTxtBox" Grid.Column="2"/>
        </Grid>
    </Grid>
</Window>
